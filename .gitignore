# Python が作る中間ファイル
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# 編集前の控え
*.bak_*

# 実収量の記録。リポジトリは公開なので出さない（config.py 第8-3節）。
# 手元の作業場にだけ置く。無くてもアプリは動く。
private_data.py

# 静的ページ版の作りかけ。
# ★まだ config.py と定数を照合していないので、いまの中身は当てにならない。
#   モデルが固まってから仕上げる。それまで公開側には出さない。
web/
